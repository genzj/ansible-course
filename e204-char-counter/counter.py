def stat(s):
    ans = _?_ # init a new, blank dict here
    _?_ # use iteration to reach each "c" of "s"
        if c _?_ ans: # judge if c has been saved in ans
            ans[c] = 1
        else:
            _?_ # increase the counter of "c"
    print(ans)

stat('The quick brown fox jumps over the lazy dog')
